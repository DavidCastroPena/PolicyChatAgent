# Example .env file for PolicyChatAgent (safe to commit)
# Copy this file to `.env` locally and replace placeholders with real keys.

GEMINI_API_KEY=<REDACTED>
SEMANTIC_SCHOLAR_API_KEY=<REDACTED>
QDRANT_HOST=localhost
QDRANT_PORT=6333
EMBEDDING_MODEL=BAAI/bge-small-en
